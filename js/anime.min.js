  anime.timeline({loop: false})
  .add({
    targets: '.landing-page-anim .line',
    opacity: [0.5,1],
    scaleX: [0, 1],
    easing: "easeInOutExpo",
    duration: 700
  }).add({
    targets: '.landing-page-anim .line',
    duration: 600,
    easing: "easeOutExpo",
    translateY: (el, i) => (-0.75 + 0.625*2.2*i) + "em"
  }).add({
    targets: '.landing-page-anim .ampersand',
    opacity: [0,1],
    scaleY: [0.5, 1],
    easing: "easeOutExpo",
    duration: 600,
    offset: '-=600'
  }).add({
    targets: '.landing-page-anim .letters-left',
    opacity: [0,1],
    translateX: ["0.5em", 0],
    easing: "easeOutExpo",
    duration: 600,
    offset: '-=300'
  }).add({
    targets: '.landing-page-anim .letters-right',
    opacity: [0,1],
    translateX: ["-0.5em", 0],
    easing: "easeOutExpo",
    duration: 600,
    offset: '-=600'
  }).add({
    targets: '.landing-page-row',
    duration: 800,
    easing: "easeOutExpo",
    opacity: [0,1],
    translateY: (el) => (0.625) + "em",
    offset: '-=300'
  });